# Benchmark Endpoints (Web Frameworks Benchmark compatible)
http.get: '/user/{id}' {
    http.response: {
        status: 200
        type: 'text/plain'
        body: $id
    }
}

http.post: '/user' {
    http.response: {
        status: 200
        type: 'text/plain'
        body: ''
    }
}
